agents:
  -
    id: canvas_page_builder_agent
    contexts: {  }
    always_include:
      - feature_page_guidelines
      - findrop_key_facts_value_propositions
  -
    id: canvas_template_builder_agent
    contexts: {  }
    always_include:
      - findrop_key_facts_value_propositions
      - media_usage_guidelines
      - report_page_guidelines
  -
    id: canvas_ai_orchestrator
    contexts: {  }
    always_include: {  }
  -
    id: drupal_canvas_seo_agent
    contexts: {  }
    always_include: {  }
