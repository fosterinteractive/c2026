description: ''
content: 'Search and select appropriate images from the media library for all page creation or editing tasks. If no suitable result is found, retry with a semantically similar query. Use default images only when no relevant media items can be found.'
